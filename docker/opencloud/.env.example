# OpenCloud Environment Configuration
# ============================================================================
# Copy this file to /var/lib/opencloud/.env and fill in the values
# 
# IMPORTANT: Set ADMIN_PASSWORD before first run!
# ============================================================================

# =============================================================================
# OPENCLOUD VERSION
# =============================================================================
# Pinned to specific version for stability and security tracking
# Check releases: https://github.com/opencloud-eu/opencloud/releases
# Update via update checker, then update this file and redeploy
OC_VERSION=5.0.2

# =============================================================================
# DOMAIN CONFIGURATION
# =============================================================================
# Domain for external access (used for generating share links, WebDAV URL, etc.)
OC_DOMAIN=cloud.somesh.dev

# =============================================================================
# ADMIN PASSWORD
# =============================================================================
# Initial admin password - MUST be set before first run!
# Generate a secure password with: openssl rand -base64 24
# 
# NOTE: This password can only be set on first initialization.
# To change it later, use the OpenCloud web UI: Settings -> Account -> Password
ADMIN_PASSWORD=CHANGE_ME_GENERATE_SECURE_PASSWORD

# =============================================================================
# STORAGE LOCATIONS
# =============================================================================
# OpenCloud configuration (on NVMe SSD for speed)
OC_CONFIG_DIR=/var/lib/opencloud/config

# OpenCloud internal data/metadata (on NVMe SSD for speed)
OC_DATA_DIR=/var/lib/opencloud/data

# User files are stored at /data/opencloud (ZFS HDD, 1TB quota)
# This is mounted directly in docker-compose.yml

# =============================================================================
# CONTAINER SETTINGS
# =============================================================================
# Port for OpenCloud web UI
OC_PORT=9200

# User/Group ID (should match NixOS tmpfiles rules)
OC_UID=1000
OC_GID=1000

# =============================================================================
# LOGGING
# =============================================================================
# Log level: debug, info, warn, error
OC_LOG_LEVEL=info

# =============================================================================
# TIMEZONE
# =============================================================================
TZ=Asia/Kolkata
