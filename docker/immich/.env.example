# Immich Environment Configuration
# ============================================================================
# Copy this file to /var/lib/immich/.env and fill in the values
# 
# IMPORTANT: Change DB_PASSWORD before first run!
# ============================================================================

# =============================================================================
# IMMICH VERSION
# =============================================================================
# Pin to specific version for stability
# Check releases: https://github.com/immich-app/immich/releases
IMMICH_VERSION=v2.4.1

# =============================================================================
# STORAGE LOCATIONS
# =============================================================================
# Photo library - ZFS dataset on USB HDD (large storage, snapshotted)
UPLOAD_LOCATION=/data/immich/photos

# Database - NVMe SSD for fast queries
DB_DATA_LOCATION=/var/lib/immich/postgres

# ML model cache - NVMe SSD for fast loading
MODEL_CACHE_LOCATION=/var/lib/immich/model-cache

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL connection settings
DB_USERNAME=postgres
DB_DATABASE_NAME=immich

# IMPORTANT: Generate a secure password!
# Use: openssl rand -base64 32
DB_PASSWORD=CHANGE_ME_GENERATE_SECURE_PASSWORD

# Database storage type optimization
# Set to HDD if postgres data is on spinning disk, SSD if on NVMe
DB_STORAGE_TYPE=SSD

# =============================================================================
# TIMEZONE
# =============================================================================
TZ=Asia/Kolkata

# =============================================================================
# OPTIONAL: EXTERNAL LIBRARY
# =============================================================================
# If you have existing photos to import, mount them here
# EXTERNAL_PATH=/mnt/existing-photos

# =============================================================================
# OPTIONAL: TELEMETRY
# =============================================================================
# Disable anonymous usage statistics (optional)
# IMMICH_TELEMETRY_INCLUDE=false
